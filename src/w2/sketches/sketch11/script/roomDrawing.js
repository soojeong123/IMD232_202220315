function setup() {
  setCanvasContainer('p5-canvas', 3, 2, true);
  background('white');
}

function draw() {
  //기본세팅
  background(255);
  rectMode(CENTER);
  fill(255);
  colorMode(RGB);
  stroke(0);
}
